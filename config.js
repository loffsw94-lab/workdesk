/* ═══════════════════════════════════════════════════════
   ★ 워크데스크 설정 파일 — 수정할 곳은 이 파일뿐입니다 ★

   각 값을 발급받은 뒤 따옴표 안에 붙여넣기만 하면
   해당 기능이 사이트 전체에서 자동으로 켜집니다.
   비워두면 그 기능만 데모 상태로 동작합니다.
   ═══════════════════════════════════════════════════════ */
window.WORKDESK_CONFIG = {

  /* 사이트 이름 (로고·문구에 사용) */
  SITE_NAME: "워크데스크",

  /* ① 구글 애드센스 — 승인받은 클라이언트 ID를 붙여넣으세요.
     넣는 순간 사이트의 모든 광고 자리가 실제 광고로 바뀝니다.
     예: "ca-pub-1234567890123456" */
  ADSENSE_CLIENT: "",

  /* ② 외주 의뢰 폼 — Formspree(formspree.io) 무료 가입 후
     발급받은 폼 주소를 붙여넣으세요. 접수 내용이 이메일로 도착합니다.
     예: "https://formspree.io/f/abcdwxyz" */
  FORM_ENDPOINT: "",

  /* ③ 네이버 뉴스 자동 수집 — 여기가 아니라 Vercel 환경변수에
     NAVER_ID / NAVER_SECRET 두 개를 넣으면 자동으로 켜집니다.
     (README.md 2단계 참고. 끄고 싶을 때만 false로 변경) */
  ENABLE_NEWS: true,

  /* ④ 실시간 환율 — 키가 필요 없는 무료 API로 자동 연동됩니다.
     끄고 싶을 때만 false로 변경 */
  ENABLE_LIVE_FX: true,

  /* ⑤ 방문자 측정(Google 애널리틱스 GA4) — analytics.google.com에서
     무료 속성 생성 후 측정 ID를 붙여넣으면 방문 통계가 켜집니다.
     예: "G-XXXXXXXXXX" */
  ANALYTICS_ID: "",

  /* ⑥ 개인정보처리방침 연락처 (선택) — 입력하면 privacy 페이지의
     보호책임자·문의처가 자동으로 채워집니다. 비워두면 기본 문구가 표시됩니다.
     ※ 애드센스 심사 전에는 이메일을 채워두는 것을 권장합니다. */
  CONTACT_NAME: "",   /* 예: "홍길동" */
  CONTACT_EMAIL: ""   /* 예: "hello@workdesk.kr" */
};
